# URL-Safety-Checker-Browser-Extension
URL Safety Checker Chrome Extension with a FastAPI (Python) backend to evaluate website safety in real time. The system checks SSL certificate validity, domain age (WHOIS), redirect chains, and affiliate/phishing indicators, then generates a risk score to classify URLs as Safe, Suspicious, or Dangerous.
